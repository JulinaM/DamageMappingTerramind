defaults:
  - paths: base
  - model: base

writer:
  base_dir: test_output
  test_name: "test1"

hydra:
  mode: MULTIRUN
  sweep:
    dir: ${writer.base_dir}/${writer.test_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  output_subdir: null
  sweeper:
    params:
      paths.trained_model: multirun/Initial2/2025-11-19/13-09-34/0/checkpoints/best_model_epoch4_valloss0.2634.pt